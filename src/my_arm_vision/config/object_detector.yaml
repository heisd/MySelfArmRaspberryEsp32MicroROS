object_detector:
  ros__parameters:
    camera_topic: /camera/image_raw
    camera_info_topic: /camera/camera_info
    detection_colors:
      - red
      - green
      - blue
    min_object_area: 500
    max_object_area: 50000
